<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.stuin.tenseconds" android:installLocation="auto">

    <application android:icon="@mipmap/icon" android:label="@string/app_name" android:theme="@style/style_app" android:allowBackup="true"
                 tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="com.stuin.tenseconds.MainActivity" android:screenOrientation="portrait" android:fullBackupContent="@xml/backup_rules">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
